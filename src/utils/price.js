export default class Price {
   constructor(unformattedPrice) {
       this.unformattedPrice = unformattedPrice
   }

   formatPrice () {
       let formattedPrice = 12.00
       return formattedPrice
   }

}